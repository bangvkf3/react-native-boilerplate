import { Animated } from "react-native";
import styled from "styled-components/native";

export const DragContainer = styled(Animated.View)`
  width: 100%;
  flex: 1;
  background-color: transparent;
`;
